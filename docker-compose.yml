go: &go
  build: dev-env
  volumes:
    - .:/go/src/stash0.eng.lancope.local/dev-infrastructure/project-lifecycle
  working_dir: /go/src/stash0.eng.lancope.local/dev-infrastructure/project-lifecycle
  environment:
    - GOOS
    - GOARCH
  entrypoint: /usr/local/go/bin/go

package:
  <<: *go
  environment:
    - GOOS=darwin
    - GOARCH=amd64
  command: ["build", "-v", "-o", "target/lc-darwin-amd64", "./main"]

publish:
  <<: *go
  environment:
    - JENKINS_GIT_KEY
    - GIT_BRANCH
  entrypoint: bash
  command: -c "/opt/publish"

test:
  <<: *go
  command: ["test", "-v", "./..."]

smoketest:
  build: smoketest
  volumes:
    - .:/opt/project/
    - /var/run/docker.sock:/var/run/docker.sock
  environment:
    - COMPOSE_PROJECT_NAME=projectlifecyclesmoketests
  working_dir: /opt/project
  entrypoint: ["rspec", "--require", "./smoketest/spec/spec_helper.rb"]
